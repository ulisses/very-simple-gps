#include "all.h"
/*
Heap *insere(Heap h[], int n)
{
	int i;
	
	for(i = 0 ; h[i] != '\0' ; )
	{
		if(h[(i - 1) / 2]);
	}
	
	return h;
}*/
/*
Heap *criaHeap()
{
	Heap *heap;
	int i;
	
	*heap = (int *) malloc(MAX*sizeof(int));
	
	for(i = 0 ; i < MAX ; i++)
		heap[i] = -1;
	
	return heap;
}*/
/*
int size(Heap h[])
{
	int i, acc=0;
	
	if(h[0] == '\0')
		return 0;
	for( i = 0, acc = 0 ; i < MAX ; i++)
	{
		if(h[2*i + 1] == '\0')
		{
			acc = 2*i +1;
			break;
		}
		if(h[2*i + 2] == '\0')
		{
			acc = 2*i + 2;
			break;
		}
	}
	
	return acc;
}*/
